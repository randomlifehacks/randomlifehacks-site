backend:
  name: github
  repo: randomlifehacks/randomlifehacks-site
  branch: main
  site_domain: randomlifehacks.com

  base_url: https://decapcms-auth.netlify.app
  auth_endpoint: auth

media_folder: "images"
public_folder: "/images"

collections:
  - name: "test"
    label: "Test Content"
    folder: "content/test"
    create: true
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Body", name: "body", widget: "markdown" }
